# GitHub Pages 上线说明

此文件夹已是可直接部署的静态站点。

1. 解压 `haoqi-design-github-pages.zip`。
2. 将解压后文件夹中的**全部内容**上传到 GitHub 仓库根目录；不要额外保留一层 `dist` 文件夹。
3. 在 GitHub 仓库的 **Settings → Pages** 中，选择从 `main` 分支的 `/ (root)` 发布。
4. 本项目使用 GitHub 个人站点仓库 `juntaoli813.github.io`，发布地址为：`https://juntaoli813.github.io/`。

`404.html` 与 `.nojekyll` 已包含在部署包中，可保证直接访问详情页链接时正确回到网站。
